# Data source: "true" (default) uses the in-memory mock; "false" hits the backend.
VITE_USE_MOCK=true

# Backend admin API base (only used when VITE_USE_MOCK=false).
VITE_API_URL=http://localhost:4000/api/v1
